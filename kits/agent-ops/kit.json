{
  "name": "agent-ops",
  "description": "Running a governed agent over long horizons — working memory that survives a context reset, deliberate handoffs, sub-agent dispatch, and the substrate a repo needs before an agent can work in it.",
  "skills": [
    "agent-notepad",
    "cold-start-generator",
    "creating-skills",
    "df-context-store",
    "df-dispatch-subagents",
    "handoff",
    "scope-init",
    "scope-retire",
    "vinculum-map"
  ],
  "hooks": [
    "check-compound-bash.sh",
    "context-budget.py"
  ],
  "harnesses": [
    "claude-code",
    "codex"
  ],
  "$excluded": {
    "handoff-auto": "REMOVED FROM TIER 1 on 2026-09-04. It was superseded by agent-notepad, whose installer already unwired it; no kit named it; and nothing outside its own directory sourced its code. The reason to remove rather than keep it was the note this entry used to carry: it shipped a SECOND copy of the hardened lib/redact.sh, and a redactor that drifts fails silently. That fork is now resolved to one copy, and test-redact-fork.sh asserts it stays that way instead of watching two copies drift."
  }
}
