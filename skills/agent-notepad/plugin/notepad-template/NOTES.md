# NOTES — working memory

<!--
  Compact working memory for this objective. Rewritten in place each cycle (not appended).
  Rules: ≤150 lines · secrets/PII redacted · plain terse markdown (never a compressed
  dialect) · never compress away a caveat. Auto-loaded on every session start.
  The append-only history lives in sessions/<ISO8601>_<id>.jsonl — this file is the summary.
-->

## Current goal

<the one thing this session is driving toward>

## Repos in scope

<abs-path (branch, role)> — see repos.manifest.json for the authoritative list

## Last decisions

- <most recent decisions, newest first; move durable/code-anchored ones to the repo store>

## Next action

<the single next concrete step a fresh agent should take>

## Open threads

- <in-flight investigations / questions not yet resolved>

## Key refs

- <repo:file:line pointers worth reloading — code anchors, not prose>

## Blockers

- <what is stopping progress; empty if none>
